# Recursive Ascent Homebrew Tap

## Install litefind

```sh
brew install recursiveascent/tap/litefind
```

Or tap this repository first:

```sh
brew tap recursiveascent/tap
brew install litefind
```

Formula updates follow litefind releases.
